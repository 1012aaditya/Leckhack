# Copy to .env and fill in once CourtListener access is approved.
# Until then, leave COURTLISTENER_TOKEN blank: the app falls back to the
# offline fixture source and everything still runs end to end.

COURTLISTENER_TOKEN=
COURTLISTENER_BASE_URL=https://www.courtlistener.com
