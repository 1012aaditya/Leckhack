# Core - everything the app needs to run offline with no credentials.
eyecite>=2.6
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
httpx>=0.27
python-multipart>=0.0.12
numpy>=1.26
pypdf>=5.0

# Optional upgrades. Each one improves a stage in place; none of them is
# required for the application to start or the demo to run.
anthropic>=0.40      # stage 2 judge (set ANTHROPIC_API_KEY)
